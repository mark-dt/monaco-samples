manifestVersion: 1.0
projects:
  - name: prod_config
  - name: test_config

environmentGroups:
  - name: production
    environments:
      - name: production-environment
        url:
          type: environment
          value: DT_PLATFORM_TENANT_URL
        auth:
          token:
            type: environment
            name: DT_API_TOKEN
