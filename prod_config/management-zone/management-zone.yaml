configs:
- id: workshop-mz
  config:
    name: Workshop MZ
    parameters:
      namespace: workshop-ns
    template: workshop-mz.json
    skip: false
  type:
    settings:
      schema: builtin:management-zones
      scope: environment

- id: workshop-mz_2
  config:
    name: Workshop MZ 2
    parameters:
      namespace: workshop-ns-2
    template: workshop-mz.json
    skip: false
  type:
    settings:
      schema: builtin:management-zones
      scope: environment

- id: workshop-mz_3
  config:
    name: Workshop MZ 3
    parameters:
      namespace: workshop-ns-3
    template: workshop-mz.json
    skip: false
  type:
    settings:
      schema: builtin:management-zones
      scope: environment